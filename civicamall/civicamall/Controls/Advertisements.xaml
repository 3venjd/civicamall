<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:pp="clr-namespace:civicamall.Controls" 
             mc:Ignorable="d"
             x:Class="civicamall.Controls.Advertisements">
    <ContentView.BindingContext>
        <pp:HeaderControlViewModel/>
    </ContentView.BindingContext>

    <CarouselView ItemsSource="{Binding Advertisements}" Position="{Binding Position}" HorizontalScrollBarVisibility="Always">
        <CarouselView.ItemTemplate>
            <DataTemplate>
                <Image Aspect="AspectFill" Source="{Binding Image}"/>
            </DataTemplate>
        </CarouselView.ItemTemplate>
    </CarouselView>
</ContentView>